import { createMuiTheme, createTheme } from "@mui/material";

export const colors = {
  light_gray: "#f3f3f3",
  md_light_gray: "#8d8d8d",
};

export const fontFamilies = {
  catamaran: "Catamaran",
};
export const theme = createTheme({});
